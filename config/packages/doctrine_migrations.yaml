doctrine_migrations:
  storage:
    table_storage:
      table_name: 'ac_migration_versions'

  migrations_paths:
    'DoctrineMigrations': '%kernel.project_dir%/database/migrations'
  enable_profiler: '%kernel.debug%'
